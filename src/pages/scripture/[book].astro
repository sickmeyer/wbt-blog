---
import type { GetStaticPaths } from 'astro';
import { bookPaths } from '../../lib/paths';
import BookView from '../../views/BookView.astro';

export const getStaticPaths = (() => bookPaths('en')) satisfies GetStaticPaths;
---

<BookView {...Astro.props} />
