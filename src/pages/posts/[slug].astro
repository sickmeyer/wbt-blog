---
import type { GetStaticPaths } from 'astro';
import { postPaths } from '../../lib/paths';
import PostView from '../../views/PostView.astro';

export const getStaticPaths = (() => postPaths('en')) satisfies GetStaticPaths;
---

<PostView {...Astro.props} />
